"Chapter_1" connexion to the server 

first we need to creat variable for the connexion to the server 

$host is the name of the server .. here the name is localhost because i'm using XAMPP

$password is the password of the user of the server .. XAMPP donen't have a default password that why we leave it empty  

$dbname is the of the database we need to connect our server to it 

try ..catch .. we use theme to handle the errors of connexion of the server 

example of the errors .. if the user write a wrong name of database or user or password 

if the server is not working  it should display the error message 

if the server is working it should display the message of connexion 